{
  "id": "$IGNORE$",
  "version": "{{version}}",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "credentialTypes": [
      {{#each credentialTypes}}  
      {
        "id": "$IGNORE$",
        "name": "$IGNORE$",
        "description": "$IGNORE$",
        "issuers": "$IGNORE$"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": $IGNORE$
}