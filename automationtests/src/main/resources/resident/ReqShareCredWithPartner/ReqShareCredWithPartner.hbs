{
  "id": "mosip.resident.share.credential",
  "version": "1.0",
  "requesttime": "{{requesttime}}",
  "request": {
    "partnerId": "{{partnerId}}",
    "sharableAttributes": [
      {
        "attributeName": "{{attributeName}}",
        "format": "{{format}}",
        "masked": {{masked}}
      }
    ],
    "purpose": "{{purpose}}",
    "consent": "{{consent}}"
  }
}