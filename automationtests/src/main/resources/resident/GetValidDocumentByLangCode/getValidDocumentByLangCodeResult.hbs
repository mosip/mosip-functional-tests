{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
  	"documentcategories": [
  		{{#each documentcategories}}
  		{
  			"code": "$IGNORE$",
            "name": "$IGNORE$",
            "description": "$IGNORE$",
            "langCode": "{{langCode}}",
            "isActive": "$IGNORE$",
            "documenttypes": [
            	{{#each documenttypes}}
            	{
            		"code": "$IGNORE$",
                    "name": "$IGNORE$",
                    "description": "$IGNORE$",
                    "langCode": "{{langCode}}",
                    "isActive": "$IGNORE$"
                }
                {{#unless @last}},{{/unless}}
                {{/each}}
            ]
        }
        {{#unless @last}},{{/unless}}
        {{/each}}    
  	]
  },
  "errors": null
}