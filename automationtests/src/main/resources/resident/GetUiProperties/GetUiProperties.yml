GetUiProperties:
   Resident_GetUiProperties_all_Valid_Smoke:
      endPoint: /resident/v1/proxy/config/ui-properties
      role: resident
      restMethod: get
      inputTemplate: resident/GetUiProperties/getUiProperties
      outputTemplate: resident/GetUiProperties/getUiPropertiesResult
      input: '{
}'
      output: '{
        "mosip.resident.api.id.auth": "{{mosip.resident.api.id.auth}}",
        "mosip.resident.api.version.otp.request": "{{mosip.resident.api.version.otp.request}}",
        "resident.datetime.pattern": "$IGNORE$",
        "mosip.mandatory-languages": "$IGNORE$",
        "mosip.resident.api.version.auth": "{{mosip.resident.api.version.auth}}",
        "mosip.optional-languages": "$IGNORE$",
        "mosip.resident.api.id.otp.request": "{{mosip.resident.api.id.otp.request}}",
        "mosip.utc-datetime-pattern": "$IGNORE$",
        "mosip.iam.adapter.clientid": "{{mosip.iam.adapter.clientid}}"
}'
