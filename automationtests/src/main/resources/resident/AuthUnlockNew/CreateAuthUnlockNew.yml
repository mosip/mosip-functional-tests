AuthUnlockNew:
   Resident_AuthUnlockNew_Valid_UIN_All:
      endPoint: /resident/v1/req/auth-type-status
      role: residentNew
      restMethod: post
      inputTemplate: resident/AuthUnlockNew/CreateAuthUnlockNew
      outputTemplate: resident/AuthUnlockNew/CreateAuthUnlockNewResult
      input: '{
        "requesttime": "$TIMESTAMP$",
        "id": "mosip.resident.authlock",
        "version": "v1",
        "authType": "demo",
        "locked": "false",
        "unlockForSeconds": "null"
    }'
      output: '{
  		"message": "Notification has been sent to the provided contact detail(s)"
}'