{
  "id": "string",
  "version": "string",
  "responsetime": "$IGNORE$",
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			       "createdBy": "$IGNORE$",
            "createdDateTime": "$IGNORE$",
			      "code": "{{code}}",
            "name": "{{name}}",
            "descr": "{{descr}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
