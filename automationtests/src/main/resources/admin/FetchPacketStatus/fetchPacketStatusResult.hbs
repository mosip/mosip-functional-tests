{
  "version": "string",
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
    "packetStatusUpdateList": [
     {{#each packetStatusUpdateList}}
      {
        "id": "{{id}}",
        "registrationId": "{{registrationId}}",
        "transactionTypeCode": "{{transactionTypeCode}}",
        "parentTransactionCode": "{{parentTransactionCode}}",
        "statusCode": "{{statusCode}}",
        "statusCommentCode": "{{statusCommentCode}}",
        "statusComment": "{{statusComment}}",
        "createdDateTimes": "$IGNORE$"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}