GenderSearch:
   Admin_TitleSearch.yml_Valid_Smoke:
      endPoint: /v1/masterdata/title/search
      role: admin
      restMethod: post
      inputTemplate: admin/search
      outputTemplate: admin/searchResult
      input: '{

	"filters": [],
	"sort": [{
		"sortField": "titleName",
		"sortType": "ASC"
	}],

	"pageStart": 0,
	"pageFetch": 10,
	"languageCode": "eng",
	"requesttime": "$TIMESTAMP$"
}'
      output: '{
   "data": [
     
    ]
}'
   Admin_TitleSearch.yml_Valid_lang:
      endPoint: /v1/masterdata/title/search
      role: admin
      restMethod: post
      inputTemplate: admin/search
      outputTemplate: admin/searchResult
      input: '{

	"filters": [
		{
		"columnName": "titleName",
		"type": "equals",
		"value": "Mr"
	}
	],
	"sort": [{
		"sortField": "titleName",
		"sortType": "ASC"
	}],

	"pageStart": 0,
	"pageFetch": 10,
	"languageCode": "eng",
	"requesttime": "$TIMESTAMP$"
}'
      output: '{
   "data": [
     
    ]
}'