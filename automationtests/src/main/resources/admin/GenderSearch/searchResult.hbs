{
  "id": "string",
  "version": "string",
  "responsetime": "$IGNORE$",
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			"createdBy": "{{createdBy}}",
            "createdDateTime": "$IGNORE$",
			"code": "{{code}}",
            "genderName": "{{genderName}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
