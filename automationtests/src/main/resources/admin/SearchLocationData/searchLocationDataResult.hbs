{
  "version": "string",
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
     {{#each data}}
      {
        "createdBy": "$IGNORE$",
        "createdDateTime": "$IGNORE$",
        "isActive": {{isActive}},
        "region": "{{region}}",
        "city": "{{city}}",
        "province": "{{province}}",
        "zone": "{{zone}}",
        "postalCode": "{{postalCode}}" 
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}