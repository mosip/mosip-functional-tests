{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "timestamp": "$IGNORE$",
  "status": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": "$IGNORE$",
    "errors": [
      {{#each errors}}
          {
            "errorCode": "{{errorCode}}",
			"message": "{{message}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}