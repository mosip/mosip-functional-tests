{
    "transactionID": "$IGNORE$",
    "version": "$IGNORE$",
    "id": "$IGNORE$",
    "errors": [
      {{#each errors}}
        {
            "errorCode": "{{errorCode}}",
            "errorMessage": "{{errorMessage}}"
            "actionMessage": "{{actionMessage}}"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ],
    "responseTime": "$IGNORE$",
    "response": {
        "authStatus": {{authStatus}}
    }
}