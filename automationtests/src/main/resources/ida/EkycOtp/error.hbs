{
  "URL": "$IGNORE$",
  "authRequest": "$IGNORE$",
  "authResponse": {
    "body": {
    "errors": [
      {{#each errors}}
        {
            "errorCode": "{{errorCode}}",
            "errorMessage": "{{errorMessage}}",
            "actionMessage": "$IGNORE$"
        }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ],
      "responseTime": "$IGNORE$",
      "response": {
        "kycStatus": {{kycStatus}}
      }
    }
  }
}