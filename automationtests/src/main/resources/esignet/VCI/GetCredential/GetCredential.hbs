{
	"client_id": "{{client_id}}",
	"idpAccessToken": "{{idpAccessToken}}",
    "format": "{{format}}",
    "credential_definition" : { 
            "types": [
	      {{#each types}}
	            "{{type}}"
	          {{#unless @last}},{{/unless}}
	      	{{/each}}
        ]
       },
    "proof": {
        "proof_type": "{{proof_type}}",
        "jwt": "{{proof_jwt}}"
    }
}