{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "responseTime": "$IGNORE$",
  "timestamp": "$IGNORE$",
  "status": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": "$IGNORE$",
    "errors": [
      {{#each errors}}
          {
            "errorCode": "{{errorCode}}",
			"errorMessage": "$IGNORE$"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}