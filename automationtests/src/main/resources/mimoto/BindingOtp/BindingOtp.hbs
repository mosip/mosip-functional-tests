{
  "requestTime": "{{requestTime}}",
  "request": {
    "individualId": "{{individualId}}",
    "otpChannels": [
      		{{#each otpChannels}}
	            "{{channel}}"
	          {{#unless @last}},{{/unless}}
	      	{{/each}}
    ]
  }
}