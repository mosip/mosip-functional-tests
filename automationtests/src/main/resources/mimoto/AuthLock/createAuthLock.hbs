 {
    "transactionID":"{{transactionID}}",
    "individualId":"{{individualId}}",
    "individualIdType":"{{individualIdType}}",
    "otp":"{{otp}}",
	"authType": [
      {{#each authType}}
            "{{auth}}"
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  }