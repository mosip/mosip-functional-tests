{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "reasonCategories": [
    {{#each reasonCategories}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "description": "{{description}}",
        "langCode": "{{langCode}}",
        "isActive": {{isActive}},
        "reasonList": [
        {{#each reasonList}}
          {
            "code": "{{code}}",
            "name": "{{name}}",
            "description": "{{description}}",
            "rsnCatCode": "{{rsnCatCode}}",
            "langCode": "{{langCode}}",
            "isActive": {{isActive}}
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
          ]
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
      ]
  },
  "errors": "$IGNORE$"
}
