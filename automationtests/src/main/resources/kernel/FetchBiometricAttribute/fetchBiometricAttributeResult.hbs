{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "biometricattributes": [
    {{#each biometricattributes}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "description": "$IGNORE$",
        "biometricTypeCode": "{{biometricTypeCode}}",
        "langCode": "{{langCode}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}