{
  "id":"$IGNORE$",
  "version":"$IGNORE$",
  "responsetime":"$IGNORE$",
  "metadata":"$IGNORE$",
  "response":{
    "appTypeCode":"{{appTypeCode}}",
    "langCode":"{{langCode}}",
    "isActive":{{isActive}},
    "documentCategories":[
{{#each documentCategories}}
      {
        "code":"{{code}}",
        "name":"{{name}}",
        "description":"{{description}}",
        "langCode":"{{langCode}}",
        "isActive":{{isActive}},
        "documentTypes":[
        {{#each documentTypes}}
          {
            "code":"{{code}}",
            "name":"{{name}}",
            "description":"{{description}}",
            "langCode":"{{langCode}}",
            "isActive":{{isActive}}
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
          ]
      }
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
      ]
  },
  "errors":"$IGNORE$"
}
