{
"responsetime": "$IGNORE$",	
"response": {
  "registrationCenters": [
  {{#each registrationCenters}}
    {
        "cntrId": "$IGNORE$",
        "machineId": "$IGNORE$",
        "usrId": "$IGNORE$",
        "isActive": "{{isActive}}",
        "langCode": "{{langCode}}",
        "effectivetimes": "$IGNORE$"
      }
     {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  },
  "errors": "$IGNORE$"
}