{
	"id": "$IGNORE$",
	"version": "$IGNORE$",
	"responsetime": "$IGNORE$",
	"metadata": null,
	"response": {
		"lastSyncTime": "$IGNORE$",
		"roles": [
     {{#each roles}}
    {
			"roleId": "{{roleId}}",
			"roleName": "{{roleName}}",
			"roleDescription": "$IGNORE$"
		}
    {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
	},
	"errors": "$IGNORE$"
}