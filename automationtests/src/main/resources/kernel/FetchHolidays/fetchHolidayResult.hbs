{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "holidays": [
      {{#each holidays}}  
      {
        "locationCode": "{{locationCode}}",
        "holidayDate": "{{holidayDate}}",
        "holidayName": "{{holidayName}}",
        "holidayDesc": "{{holidayDesc}}",
        "langCode": "{{langCode}}",
        "isActive": {{isActive}}
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}