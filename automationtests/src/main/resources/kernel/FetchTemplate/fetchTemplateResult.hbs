{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
  "templates": [
  {{#each templates}}
    {
      "id": "$IGNORE$",
      "name": "{{name}}",
      "description": "$IGNORE$",
      "fileFormatCode": "{{fileFormatCode}}",
      "model": "$IGNORE$",
      "fileText": "$IGNORE$",
      "moduleId": "{{moduleId}}",
      "moduleName": "{{moduleName}}",
      "templateTypeCode": "{{templateTypeCode}}",
      "langCode": "{{langCode}}",
      "isActive": {{isActive}}
    }
    {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
},
  "errors": "$IGNORE$"
}