{
  "id": "string",
  "version": "string",
  "responsetime": "$IGNORE$",
    "errors": [
      {{#each errors}}
          {
            "errorCode": "{{errorCode}}",
			"message": "{{message}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}