{
  "genderType": [
  {{#each genderType}}
    {
      "code": "{{code}}",
      "genderName": "{{genderName}}",
      "langCode": "{{langCode}}",
      "isActive": {{isActive}}
    }
    {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}