{
  "id": "string",
  "metadata": {},
  "request": {
    "filters": [
    {{#each filters}}
      {
        "columnName": "{{columnName}}",
        "text": "{{text}}",
        "type": "{{type}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ],
    "optionalFilters": [
      {
        "columnName": "$IGNORE$",
        "fromValue": "$IGNORE$",
        "toValue": "$IGNORE$",
        "type": "$IGNORE$",
        "value": "$IGNORE$"
      }
    ],
     "purpose": "{{purpose}}"
  },
  "requesttime": "2018-12-10T06:12:52.994Z",
  "version": "string"
}