createApproveRejectDeviceDetail:
   Partner_ApproveRejectDeviceDetail_All_Valid_Smoke:
      endPoint: /partnermanagement/v1/partners/devicedetail
      role: partner
      restMethod: patch
      inputTemplate: partner/DeviceDetail/ApproveRejectDeviceDetail/createApproveRejectDeviceDetail
      outputTemplate: partner/DeviceDetail/ApproveRejectDeviceDetail/createApproveRejectDeviceDetailResult
      input: '{
    "approvalStatus": "Activate",
    "id": "device-id-123",
    "isItForRegistrationDevice": "true",
    "requesttime": "$TIMESTAMP$"
  }'
      output: '{
        "id": "Device details approved successfully."
    }'

   Partner_ApproveRejectDeviceDetail_Missing_InputParameter_approvalStatus:
      endPoint: /partnermanagement/v1/partners/devicedetail
      role: partner
      restMethod: patch
      inputTemplate: partner/DeviceDetail/ApproveRejectDeviceDetail/createApproveRejectDeviceDetail
      outputTemplate: partner/error
      input: '{
    "approvalStatus": "$REMOVE$",
    "id": "device-id-123",
    "isItForRegistrationDevice": "true",
    "requesttime": "$TIMESTAMP$"
  }'
      output: '{
   "errors": [
    {
      "errorCode": "PMS_COR_001",
      "message": "Missing Input Parameter"
    }
  ]
}'


   Partner_ApproveRejectDeviceDetail_Missing_InputParameter_deviceId:
      endPoint: /partnermanagement/v1/partners/devicedetail
      role: partner
      restMethod: patch
      inputTemplate: partner/DeviceDetail/ApproveRejectDeviceDetail/createApproveRejectDeviceDetail
      outputTemplate: partner/error
      input: '{
    "approvalStatus": "Activate",
    "id": "$REMOVE$",
    "isItForRegistrationDevice": "true",
    "requesttime": "$TIMESTAMP$"
  }'
      output: '{
   "errors": [
    {
      "errorCode": "PMS_COR_001",
      "message": "Missing Input Parameter"
    }
  ]
}'