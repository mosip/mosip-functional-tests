{
  "request": {
    "name": "{{name}}",
    "policyName": "{{policyName}}",
    "publicKey": {{publicKey}},
    "authPartnerId": "{{authPartnerId}}",
    "logoUri": "{{logoUri}}",
    "redirectUris": [
          "{{redirectUris}}"
  ],
    "grantTypes": [
            "{{grantTypes}}"
        ],
    "clientAuthMethods": [
            "{{clientAuthMethods}}"
        ]
  }
}