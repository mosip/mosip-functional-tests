{
  "clientId": "{{clientId}}",
  "id": "string",
  "version": "string",
  "requestTime": "{{requestTime}}",
  "request": {
    "clientName": "{{clientName}}",
    "status": "{{status}}",
    "logoUri": "{{logoUri}}",
    "redirectUris": [
      "{{redirectUris}}"
    ],
    "userClaims": [
			"{{userClaims1}}",
            "{{userClaims2}}",
            "{{userClaims3}}",
            "{{userClaims4}}",
            "{{userClaims5}}"
    ],
    "authContextRefs": [
      "{{authContextRefs}}"
    ],
    "grantTypes": [
      "{{grantTypes}}"
    ],
    "clientAuthMethods": [
      "{{clientAuthMethods}}"
    ]
  }
}'