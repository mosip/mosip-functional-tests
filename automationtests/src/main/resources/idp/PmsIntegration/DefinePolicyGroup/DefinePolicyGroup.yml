definePolicyGroup:
   Idp_DefinePolicyGroup_All_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$POLICYGROUPDESC$",
    "name": "$POLICYGROUPNAME$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
    }'
   Idp_DefinePolicyGroup_in_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$RID$",
    "name": "$RID$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
    }'
   Idp_DefinePolicyGroup_Valid_Smoke_sid:
      endPoint: /v1/policymanager/policies/group/new
      role: partner
      restMethod: post
      inputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroup
      outputTemplate: idp/PmsIntegration/DefinePolicyGroup/definePolicyGroupResult
      input: '{
    "desc": "$RIDDEL$",
    "name": "$RIDDEL$",
    "requesttime": "$TIMESTAMP$"
  }'
      output: ' {
      "id": "$IGNORE$",
        "name": "$IGNORE$",
        "desc": "$IGNORE$",
        "is_Active": true
    }'