GetOidcClient:
   Idp_GetOidcClient_all_Valid_Smoke:
      endPoint: /v1/partnermanager/oidc/client/{client_id}
      role: resident
      restMethod: get
      inputTemplate: idp/GetOidcClient/GetOidcClient
      outputTemplate: idp/GetOidcClient/GetOidcClientResult
      input: '{
      	"client_id": "$ID:CreateOIDCClient_all_Valid_Smoke_sid_clientId$"
}'
      output: '{
      
}'

   Idp_GetOidcClient_Invalid_clientId_Neg:
      endPoint: /v1/partnermanager/oidc/client/{client_id}
      role: resident
      restMethod: get
      inputTemplate: idp/GetOidcClient/GetOidcClient
      outputTemplate: idp/error
      input: '{
      	"client_id": "$sjdh@#$"
}'
      output: '{
  "errors": [
    {
      "errorCode": "PMS_IDP_004",
      "errorMessage": "$IGNORE$"
    }
  ]
}'