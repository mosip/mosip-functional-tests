{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "timestamp": "$IGNORE$",
  "status": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": "$IGNORE$",
    "errors": [
      {{#each errors}}
          {
            "registrationId": "{{registrationId}}",
            "errorCode": "{{errorCode}}",
			"message": "{{message}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}