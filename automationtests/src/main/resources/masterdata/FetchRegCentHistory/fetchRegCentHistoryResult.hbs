{
"response": {
  "registrationCentersHistory": [
  {{#each registrationCentersHistory}}
    {
        "langCode": "{{langCode}}",
        "isActive": "{{isActive}}"
      }
     {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  }
}