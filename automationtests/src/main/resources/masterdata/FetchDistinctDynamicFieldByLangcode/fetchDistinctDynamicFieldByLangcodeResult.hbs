{
    "id": "$IGNORE$",
    "version": "$IGNORE$",
    "responsetime": "$IGNORE$",
    "metadata": "$IGNORE$",
    "response": [
    	{{#each response}} 
        {
	        "name": "$IGNORE$",
	        "description": "$IGNORE$",
	        "isActive": "$IGNORE$"
        }
        {{#unless @last}},{{/unless}}
      	{{/each}}
    ],
    "errors": null
}