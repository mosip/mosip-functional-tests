{
  "response": {
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "word": "{{word}}",
        "langCode": "{{langCode}}",
        "description": "{{description}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}