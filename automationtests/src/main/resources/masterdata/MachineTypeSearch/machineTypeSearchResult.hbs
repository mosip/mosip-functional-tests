{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "createdBy": "{{createdBy}}",
        "createdDateTime": "$IGNORE$",
        "updatedBy": "$IGNORE$",
        "updatedDateTime": "$IGNORE$",
        "isDeleted": "$IGNORE$",
        "deletedDateTime": "$IGNORE$",
        "code": "{{code}}",
        "langCode": "{{langCode}}",
        "name": "{{name}}",
        "description": "{{description}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}