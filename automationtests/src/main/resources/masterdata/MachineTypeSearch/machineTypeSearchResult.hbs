{
  "response": {
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "createdBy": "{{createdBy}}",
        "code": "{{code}}",
        "langCode": "{{langCode}}",
        "name": "{{name}}",
        "description": "{{description}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}