{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "biometricattributes": [
	
    {{#each biometricattributes}}
      {
        "code": "$IGNORE$",
        "name": "$IGNORE$",
        "description": "$IGNORE$",
        "biometricTypeCode": "{{biometricTypeCode}}",
        "langCode": "{{langCode}}",
        "isActive": $IGNORE$
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}