{
  "response": {
    "biometricattributes": [
	
    {{#each biometricattributes}}
      {
        "biometricTypeCode": "{{biometricTypeCode}}",
        "langCode": "{{langCode}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}