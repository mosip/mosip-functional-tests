{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "reasonCategories": [
	    {{#each reasonCategories}}
	      {
	        "code": "$IGNORE$",
	        "name": "$IGNORE$",
	        "description": "$IGNORE$",
	        "langCode": "$IGNORE$",
	        "isActive": "$IGNORE$",
			"reasonList": [
			    {{#each reasonList}}
			      {	    
					"code": "$IGNORE$",
			        "name": "$IGNORE$",
			        "description": "$IGNORE$",
			        "rsnCatCode": "$IGNORE$",
			        "langCode": "$IGNORE$",
			        "isActive": "$IGNORE$"
			      }
			     {{#unless @last}},{{/unless}}
			     {{/each}}
		    ]
	      }
	    {{#unless @last}},{{/unless}}
	    {{/each}}
    ]
  },
  "errors": null
}