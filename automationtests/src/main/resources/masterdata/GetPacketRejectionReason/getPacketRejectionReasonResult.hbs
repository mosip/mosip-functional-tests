{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
    "reasonCategories": [
	
    {{#each reasonCategories}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "description": "{{description}}",
        "langCode": "{{langCode}}",
        "isActive": {{langCode}},
	"reasonList": [
	
    {{#each reasonList}}
      {	    
	"code": "{{code}}",
        "name": "{{name}}",
        "description": "{{description}}",
        "rsnCatCode": "{{rsnCatCode}}",
        "langCode": "{{langCode}}",
        "isActive": {{langCode}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}