{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
    "errors": [
      {{#each errors}}
          {
            "errorCode": "{{errorCode}}",
			"message": "$IGNORE$"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}