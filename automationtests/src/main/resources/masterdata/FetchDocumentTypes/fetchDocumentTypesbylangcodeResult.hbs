{
  "response": {
    "documenttypes": [
      {{#each documenttypes}}  
      {
        "langCode": "{{langCode}}",
        "isActive": "{{isActive}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}