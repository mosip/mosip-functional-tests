{
  "response": {
   "data": [
     {{#each data}}
      {
         "userId": "{{userId}}",
        "zoneName": "{{zoneName}}",
        "regCenterId": "{{regCenterId}}",
        "zoneCode": "{{zoneCode}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}