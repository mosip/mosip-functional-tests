{
  "response": {
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			"code": "{{code}}",
            "titleName": "{{titleName}}",
            "titleDescription": "{{titleDescription}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
