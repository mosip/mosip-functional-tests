{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "locations": [
	
    {{#each locations}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "hierarchyLevel": "{{hierarchyLevel}}",
        "hierarchyName": "{{hierarchyName}}",
        "parentLocCode": "{{parentLocCode}}",
        "langCode": "{{langCode}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}