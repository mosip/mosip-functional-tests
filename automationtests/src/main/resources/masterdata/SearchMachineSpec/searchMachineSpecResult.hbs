{
  "response": {
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "id": "{{id}}",
        "langCode": "{{langCode}}",
        "name": "{{name}}",
        "description": "{{description}}",
        "model": "{{model}}",
        "brand": "{{brand}}",
        "machineTypeCode": "{{machineTypeCode}}",
        "minDriverversion": "{{minDriverversion}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}