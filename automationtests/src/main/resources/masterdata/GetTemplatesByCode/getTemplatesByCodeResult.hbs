{
  "response": {
  "templates": [
    {{#each templates}}
    {
        "templateTypeCode": "{{templateTypeCode}}",
        "isActive": "{{isActive}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": $IGNORE$
}