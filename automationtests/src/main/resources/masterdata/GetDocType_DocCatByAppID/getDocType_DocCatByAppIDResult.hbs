{
  "response":{
    "langCode":"{{langCode}}",
    "isActive":{{isActive}},
    "documentCategories":[
{{#each documentCategories}}
      {
        "langCode":"{{langCode}}",
        "isActive":{{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
      ]
  }
}
