{
  "response": {
    "templateFileFormats": [
    {{#each templateFileFormats}}
      {
        "code": "{{code}}",
        "isActive": "{{isActive}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}