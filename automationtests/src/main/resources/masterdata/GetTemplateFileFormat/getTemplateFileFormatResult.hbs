{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "templateFileFormats": [
    {{#each templateFileFormats}}
      {
        "code": "{{code}}",
        "description": "$IGNORE$",
        "langCode": "$IGNORE$",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": null
}