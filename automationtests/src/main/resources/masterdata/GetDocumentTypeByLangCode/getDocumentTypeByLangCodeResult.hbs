{
  "response": {
    "documenttypes": [
	
    {{#each documenttypes}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "description": "{{description}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}