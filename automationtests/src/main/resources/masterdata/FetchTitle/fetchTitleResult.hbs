{
  "response": {
    "titleList": [
    {{#each titleList}}
      {
        "isActive": {{isActive}},
        "langCode": "{{langCode}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}