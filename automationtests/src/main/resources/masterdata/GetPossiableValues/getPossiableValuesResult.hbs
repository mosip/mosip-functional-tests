{
  "response": {
    "eng": [
    {{#each eng}}
      {
        "code": "{{code}}",
        "value": "{{value}}",
        "dataType": "{{dataType}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}