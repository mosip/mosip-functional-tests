{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "eng": [
    {{#each eng}}
      {
        "code": "{{code}}",
        "value": "{{value}}",
        "dataType": "{{dataType}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}