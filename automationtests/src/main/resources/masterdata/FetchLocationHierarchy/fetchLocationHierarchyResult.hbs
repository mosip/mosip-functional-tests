{
"response": {
  "locationHierarchyLevels": [
  {{#each locationHierarchyLevels}}
    {
        "langCode": "{{langCode}}",
        "isActive": "{{isActive}}"
      }
     {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  }
}