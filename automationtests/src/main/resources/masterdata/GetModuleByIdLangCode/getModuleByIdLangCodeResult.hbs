{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "modules": [
	
    {{#each modules}}
      {
        "id": "{{id}}",
        "name": "{{name}}",
        "langCode": "$IGNORE$",
        "description": "{{description}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}