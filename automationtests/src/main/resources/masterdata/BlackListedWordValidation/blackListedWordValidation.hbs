{
  "id": "string",
  "metadata": {},
  "request": {
    "blacklistedwords": [
      {{#each blacklistedwords}}
            "{{word}}"
          {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  },
  "requesttime": "{{requesttime}}",
  "version": "{{version}}"
}