{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
  "weekdays": [
    {{#each weekdays}}
      {
      	"name": "$IGNORE$",
        "dayCode": "$IGNORE$",
        "languageCode": "{{languageCode}}",
        "working": $IGNORE$,
        "globalWorking": $IGNORE$
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": $IGNORE$
}