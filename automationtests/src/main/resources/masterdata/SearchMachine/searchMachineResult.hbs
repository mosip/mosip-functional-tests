{
  "response": {
    "data": [
     {{#each data}}
      {
        "id": "{{id}}",
        "name": "{{name}}",
        "serialNum": "{{serialNum}}",
        "macAddress": "{{macAddress}}",
        "ipAddress": "{{ipAddress}}",
        "machineSpecId": "{{machineSpecId}}",
        "langCode": "{{langCode}}",
        "zoneCode": "{{zoneCode}}",
        "zone": "{{zone}}",
        "isActive": "{{isActive}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}