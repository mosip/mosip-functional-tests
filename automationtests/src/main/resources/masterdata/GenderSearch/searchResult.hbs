{
  "response": {
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			"code": "{{code}}",
            "genderName": "{{genderName}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
