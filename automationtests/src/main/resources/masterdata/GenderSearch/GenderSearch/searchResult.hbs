{
  "id": "string",
  "version": "string",
  "responsetime": "$IGNORE$",
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			"createdBy": "$IGNORE$",
            "createdDateTime": "$IGNORE$",
			"code": "{{code}}",
            "genderName": "{{genderName}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
