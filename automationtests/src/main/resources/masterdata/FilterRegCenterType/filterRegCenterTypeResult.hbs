{
  "id": "string",
  "version": "string",
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
    "filters": [
     {{#each filters}}
      {
        "fieldID": "{{fieldID}}",
        "fieldValue": "{{fieldValue}}",
        "fieldCode":"{{fieldCode}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": []
}