{
"response": {
  "devices": [
  {{#each devices}}
    {
			"name": "{{name}}",
			"serialNum": "{{serialNum}}",
			"deviceSpecId": "{{deviceSpecId}}",
			"macAddress": "{{macAddress}}",
			"langCode": "{{langCode}}",
			"isActive": {{isActive}},
            "deviceTypeCode": "{{deviceTypeCode}}",
			"zoneCode": "{{zoneCode}}",
			"regCenterId": "{{regCenterId}}"
		}
     {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
}
}