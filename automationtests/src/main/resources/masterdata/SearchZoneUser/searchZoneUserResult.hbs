{
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "fromRecord": "$IGNORE$",
    "toRecord": "$IGNORE$",
    "totalRecord": "$IGNORE$",
    "data": [
     {{#each data}}
      {
        "zoneCode": "{{zoneCode}}",
        "userId": "{{userId}}",
        "langCode": "{{langCode}}",
        "userName": "{{userName}}",
        "zoneName": "{{zoneName}}" 
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}