{
  "response": {
    "data": [
     {{#each data}}
      {
        "zoneCode": "{{zoneCode}}",
        "userId": "{{userId}}",
        "langCode": "{{langCode}}",
        "userName": "{{userName}}",
        "zoneName": "{{zoneName}}" 
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}