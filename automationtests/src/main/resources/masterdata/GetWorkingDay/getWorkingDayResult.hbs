{
  "id": null,
  "version": null,
  "responsetime": "$IGNORE$",
  "metadata": null,
  "response": {
  "workingdays": [
    {{#each workingdays}}
      {
      	"code": "$IGNORE$",
        "name": "$IGNORE$",
        "order": $IGNORE$,
        "languageCode": "{{languageCode}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": $IGNORE$
}