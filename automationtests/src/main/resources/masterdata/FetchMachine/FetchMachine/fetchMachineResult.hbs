{
"responsetime": "$IGNORE$",	
"response": {
  "machines": [
  {{#each machines}}
    {
        "id": "$IGNORE$",
        "name": "$IGNORE$",
        "serialNum": "$IGNORE$",
        "macAddress": "$IGNORE$",
        "ipAddress": "$IGNORE$",
        "machineSpecId": "$IGNORE$",
        "regCenterId": "$IGNORE$",
        "langCode": "$IGNORE$",
        "isActive": "{{isActive}}",
        "validityDateTime": "$IGNORE$",
        "publicKey": "$IGNORE$",
        "signPublicKey": "$IGNORE$"
      }
     {{#unless @last}},{{/unless}}
      	{{/each}}
  ]
  },
  "errors": "$IGNORE$"
}