{
	"id": "mosip.applicanttype.fetch",
	"metadata": {},
	"request": {
		"attributes": [
    {{#each attributes}}
    {
				"attribute": "{{attribute}}",
				"value": "{{value}}"
		}
    {{#unless @last}},{{/unless}}
      	{{/each}}
		]
	},
	"requesttime": "{{requesttime}}",
	"version": "1.0"
}