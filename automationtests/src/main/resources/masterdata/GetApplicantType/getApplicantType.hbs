{
	"id": "getapplicanttype.code",
	"metadata": {},
	"request": {
		"attributes": [
    {{#each attributes}}
    {
				"attribute": "{{attribute}}",
				"value": "{{value}}"
		}
    {{#unless @last}},{{/unless}}
      	{{/each}}
		]
	},
	"requesttime": "{{requesttime}}",
	"version": "V1.0"
}