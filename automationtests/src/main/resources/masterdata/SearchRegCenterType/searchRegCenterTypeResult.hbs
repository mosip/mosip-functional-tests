{
  "response": {
    "data": [
      {{#each data}}
          {
            "isActive": "{{isActive}}",
			      "code": "{{code}}",
            "name": "{{name}}",
            "descr": "{{descr}}",
            "langCode": "{{langCode}}"
          }
          {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
