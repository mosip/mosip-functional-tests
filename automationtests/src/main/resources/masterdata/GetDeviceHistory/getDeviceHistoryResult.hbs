{
  "response": {
    "deviceHistoryDetails": [
       {{#each deviceHistoryDetails}}
      {
        "langCode": "{{langCode}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}
