{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "deviceHistoryDetails": [
       {{#each deviceHistoryDetails}}
      {
        "id": "$IGNORE$",
        "name": "$IGNORE$",
        "serialNum": "$IGNORE$",
        "ipAddress": "$IGNORE$",
        "macAddress": "$IGNORE$",
        "deviceSpecId": "$IGNORE$",
        "langCode": "{{langCode}}",
        "isActive": {{isActive}},
        "effectDateTime": "$IGNORE$",
        "validityDateTime": "$IGNORE$"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}
