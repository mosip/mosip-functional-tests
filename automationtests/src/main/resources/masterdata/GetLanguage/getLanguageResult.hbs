{
  "response": {
    "languages": [
	
    {{#each languages}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "family": "{{family}}",
        "nativeName": "{{nativeName}}",
        "isActive": "{{isActive}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}