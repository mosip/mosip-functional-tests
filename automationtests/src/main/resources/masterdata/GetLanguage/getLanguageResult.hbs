{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "languages": [
	
    {{#each languages}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "family": "{{family}}",
        "nativeName": "{{nativeName}}",
        "isActive": {{isActive}}
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}