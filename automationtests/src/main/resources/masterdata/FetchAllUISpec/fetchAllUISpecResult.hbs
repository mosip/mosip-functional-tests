{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": [
    {
      "id": "$IGNORE$",
      "version": "$IGNORE$",
      "title": "$IGNORE$",
      "description": "$IGNORE$",
      "identitySchemaId": "$IGNORE$",
      "idSchemaVersion": "$IGNORE$",
      "domain": "$IGNORE$",
      "jsonSpec": [
        {
          "type": "$IGNORE$",
          "spec": [
           {{#each spec}}
            {
              "name": "$IGNORE$",
              "description": "$IGNORE$",
              "label": "$IGNORE$",
              "fxml": "$IGNORE$",
              "icon": "$IGNORE$",
              "order": "$IGNORE$",
              "shortcut-icon": "$IGNORE$",
              "access-control": "$IGNORE$"
            }
            {{#unless @last}},{{/unless}}
      	{{/each}}
          ]
        }
      ],
      "status": "$IGNORE$",
      "effectiveFrom": "$IGNORE$",
      "createdBy": "$IGNORE$",
      "updatedBy": "$IGNORE$",
      "createdOn": "$IGNORE$",
      "updatedOn": "$IGNORE$"
    }
  ],
  "errors": "$IGNORE$"
}