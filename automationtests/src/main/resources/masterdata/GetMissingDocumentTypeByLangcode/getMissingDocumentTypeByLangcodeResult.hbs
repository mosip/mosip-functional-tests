{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": [
    {{#each response}}
      {
      "id": "{{id}}",
      "fieldValue": "$IGNORE$",
      "langCode": "$IGNORE$"
    }
    {{#unless @last}},{{/unless}}
      	{{/each}}
  ],
  "errors": "$IGNORE$"
}