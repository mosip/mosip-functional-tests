{
  "id": "$IGNORE$",
  "version": "$IGNORE$",
  "responsetime": "$IGNORE$",
  "metadata": "$IGNORE$",
  "response": {
    "workingdays": [
	{{#each workingdays}}
      {
        "code": "$IGNORE$",
        "name": "$IGNORE$",
        "order": $IGNORE$,
        "languageCode": "{{languageCode}}"
      }
 	{{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  },
  "errors": "$IGNORE$"
}