{
  "response": {
    "data": [
     {{#each data}}
      {
        "isActive": "{{isActive}}",
        "code": "{{code}}",
        "name": "{{name}}",
        "langCode": "{{langCode}}",
        "description": "{{description}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}