{
  "response": {
    "registrationCenters": [
	
    {{#each registrationCenters}}
      {
        "cntrId": "{{cntrId}}",
        "machineId": "{{machineId}}",
        "usrId": "{{usrId}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}