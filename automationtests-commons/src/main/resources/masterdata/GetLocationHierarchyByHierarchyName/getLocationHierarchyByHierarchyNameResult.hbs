{
  "response": {
    "locations": [
	
    {{#each locations}}
      {
        "code": "{{code}}",
        "name": "{{name}}",
        "hierarchyLevel": "{{hierarchyLevel}}",
        "hierarchyName": "{{hierarchyName}}",
        "parentLocCode": "{{parentLocCode}}",
        "isActive": "{{isActive}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}