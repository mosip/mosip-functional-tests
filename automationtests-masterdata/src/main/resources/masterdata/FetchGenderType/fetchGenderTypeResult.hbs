{
  "response": {
    "genderType": [
      {{#each genderType}}  
      {
        "langCode": "{{langCode}}",
        "isActive": "{{isActive}}"
      }
       {{#unless @last}},{{/unless}}
      	{{/each}}
    ]
  }
}