{
	"id": "string",
	"metadata": {},
	"request": {
		 "filters": [
    {{#each filters}}
      {
        "columnName": "{{columnName}}",
        "type": "{{type}}",
        "value": "{{value}}"
      }
      {{#unless @last}},{{/unless}}
      	{{/each}}
    ],
		"sort": [
			{
				"sortType": "desc",
				"sortField": "createdDateTime"
			}
		],
		"pagination": {
			"pageStart": 0,
			"pageFetch": 10
		},
		"languageCode": "{{languageCode}}"
	},
	"version": "string",
	"requesttime": "{{requesttime}}"
}