{
  "id": "{{id}}",
  "request": {
	"authTypes": [
            {
		        "authType": "{{authType}}",
		        "locked": "{{locked}}",
		        "unlockForSeconds": "{{unlockForSeconds}}"
      		}
  ]
  },
  "requesttime": "{{requesttime}}",
  "version": "{{version}}"
}