package mosip.api.services;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * This class is where all re-usable common asserts are held. Below the commons
 * are the application specific assertions They are all re-usable and
 * generically named for readability
 */

public class AssertLibrary  {
	
	// GLOBAL CLASS VARIABLES
		private static Logger logger = LogManager.getLogger(AssertLibrary.class);
		
		//common assert functions

}
